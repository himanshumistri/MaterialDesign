<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.himotech.matrialdesign" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/DesignTheme" >
        <activity
            android:name=".HomeActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CollapseToolBarActivity"
            android:label="@string/title_activity_collapse_tool_bar" >
        </activity>
        <activity
            android:name=".NavigationDraweActivity"
            android:label="@string/title_activity_navigation_drawe" >
        </activity>
        <activity
            android:name=".DrawerNewActivity"
            android:label="@string/title_activity_drawer_new" >
        </activity>
    </application>

</manifest>
